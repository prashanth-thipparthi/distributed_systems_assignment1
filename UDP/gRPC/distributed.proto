syntax = "proto3";

package distributed_systems_assignment1.UDP.gRPC;

service TimeServer {
   // Obtains the Message and returns the server times.
    rpc GetTime(Msg) returns (Times) {}
}

message Msg {
  string latitude = 1;
}

message Times {
  uint64 received = 1;
  uint64 sent = 2;
}